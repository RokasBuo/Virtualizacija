server {
  listen 80 default_server;
  listen [::]:80 default_server;
  root /var/www/website;
  index index.html;
  server_name 193.219.91.103:7096;
  location / {
    try_files $uri $uri/ =404;
  }
}
